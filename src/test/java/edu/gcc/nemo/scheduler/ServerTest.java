package edu.gcc.nemo.scheduler;

import static org.junit.jupiter.api.Assertions.*;

import edu.gcc.nemo.scheduler.DB.Admins;
import edu.gcc.nemo.scheduler.DB.Courses;
import edu.gcc.nemo.scheduler.DB.Students;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;
import org.springframework.boot.SpringApplication;

class ServerTest {
//
//    @BeforeAll
//    static void setUp() {
//        //Initialize the Spring application context
//        SpringApplication.run(Session.class);
//        Server server = new Server();
//    }
//
//    @Test
//    void testSingletonObjects() {
//        // Verify that the singleton objects are not null
//        assertNotNull(Server);
//        assertNotNull(server.admins);
//        assertNotNull(Server.courses);
//    }
//
//    @Test
//    void testSession() {
//        //Verify that the session is created with the correct parameters
//        Session session = new Session(Admins.getAdminsInstance(), Students.getInstance(), Courses.getInstance());
//        assertSame(Server.class, session.);
//        assertSame(Server.students, session.getStudents());
//        assertSame(Server.courses, session.getCourses());
//    }
}